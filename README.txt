Keeping track of changes made to the WordPress Plugin Social Sharing Toolkit
http://wordpress.org/plugins/social-sharing-toolkit/

Initial change is to change default behavior which Pins the "featured-image" which is often a thumbnail for theme layout purposes.  We're changing it to Pin the first post image simply by removing the code which gets the thumbnail.